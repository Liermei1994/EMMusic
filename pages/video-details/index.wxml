<wxs src="../../utils/format.wxs" module="format"></wxs>
<video 
  class="video"
  src="{{mvUrl.url}}"
  autoplay
  loop
  referrer-policy="origin"
>
</video>
<!-- mv信息 -->
<view class="mvInfos">
  <view class="top">{{mvInfo.name}}</view>
  <view class="same center">{{mvInfo.artistName}}</view>
  <view class="same bottom">{{format.formatCount(mvInfo.playCount)}}次播放 - {{mvInfo.publishTime}}</view>
</view>

<!-- 推荐视频 -->
<view class="recommandMvs">
  <view class="rm-title">推荐视频</view>
  <view></view>
</view>